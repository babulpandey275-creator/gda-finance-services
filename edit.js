import { db } from "./firebase.js"; import { doc, getDoc, updateDoc } from "https://www.gstatic.com/firebasejs/12.0.0/firebase-firestore.js"; window.addEventListener('DOMContentLoaded', async () => { const urlParams = new URLSearchParams(window.location.search); const custId = urlParams.get('id'); if (!custId) { alert("ग्राहक ID नहीं मिली!"); window.location.href = "customer-list.html"; return; } const adminPassword = prompt("🔐 सुरक्षा लॉक: ग्राहक का डेटा एडिट करने के लिए एडमिन पासवर्ड डालें:"); if (adminPassword !== "GDA@2026") { alert("❌ गलत पासवर्ड! आप इस पेज को एक्सेस नहीं कर सकते।"); window.location.href = "customer-list.html"; return; } async function loadCustomerData() { try { const docSnap = await getDoc(doc(db, "customers", custId)); if (!docSnap.exists()) { alert("ग्राहक रिकॉर्ड नहीं मिला!"); window.location.href = "customer-list.html"; return; } const cust = docSnap.data(); if(document.getElementById("customerName")) document.getElementById("customerName").value = cust.name || ""; if(document.getElementById("mobileNumber")) document.getElementById("mobileNumber").value = cust.mobile || ""; if(document.getElementById("address")) document.getElementById("address").value = cust.address || ""; if(document.getElementById("aadhaarNumber")) { const dbAadhar = cust.aadharCard || cust.aadhaar || cust.aadhar || ""; document.getElementById("aadhaarNumber").value = dbAadhar; } if(document.getElementById("panNumber")) document.getElementById("panNumber").value = cust.panCard || cust.pan || ""; if(document.getElementById("loanAmount")) document.getElementById("loanAmount").value = cust.loanAmount || ""; if(document.getElementById("loanPlan")) document.getElementById("loanPlan").value = cust.planDuration || cust.duration || ""; if(document.getElementById("emi")) document.getElementById("emi").value = cust.dailyEmi || cust.emi || ""; } catch (err) { console.error(err); alert("⚠️ डेटा लोड करने में समस्या आई।"); } } await loadCustomerData(); const saveBtn = document.getElementById("saveBtn") || document.querySelector("button[type='submit']"); if (saveBtn) { saveBtn.onclick = async (e) => { e.preventDefault(); const name = document.getElementById("customerName").value.trim(); const mobile = document.getElementById("mobileNumber").value.trim(); const address = document.getElementById("address").value.trim(); const aadhaar = document.getElementById("aadhaarNumber").value.trim(); const pan = document.getElementById("panNumber") ? document.getElementById("panNumber").value.trim().toUpperCase() : "-"; const loanAmount = Number(document.getElementById("loanAmount").value); const planDuration = Number(document.getElementById("loanPlan").value); const emi = Number(document.getElementById("emi").value); if (!name || !mobile || !address || !aadhaar || !loanAmount) { alert("⚠️ कृपया सभी जरूरी जानकारी दर्ज करें!"); return; } try { saveBtn.disabled = true; saveBtn.innerText = "⏳ अपडेट हो रहा है..."; const totalCollection = loanAmount + (loanAmount * 0.20); await updateDoc(doc(db, "customers", custId), { name: name, mobile: mobile, address: address, aadharCard: aadhaar, aadhaar: aadhaar, aadhar: aadhaar, panCard: pan, loanAmount: loanAmount, planDuration: planDuration, duration: planDuration, dailyEmi: emi, emi: emi, totalCollection: totalCollection }); alert("🎉 ग्राहक का डेटा सफलतापूर्वक अपडेट हो गया है!"); window.location.href = "customer-list.html"; } catch (error) { console.error(error); alert("⚠️ डेटाबेस अपडेट एरर।"); saveBtn.disabled = false; saveBtn.innerText = "💰 अपडेट सुरक्षित करें"; } }; } });
